
  // char* word;
  // word = strtok(operand, "'");
  // printf("1st word: %s\n", word);
  // strcpy(first_register, word);
  // word = strtok(NULL, "'");
  // printf("2nd word: %s\n", word);
  // strcpy(second_register, word);
  // word = strtok(NULL, " \t\n");
  // printf("3rd word: %s\n", word);